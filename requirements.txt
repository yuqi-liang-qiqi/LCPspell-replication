# Pinned versions for reproducible paper replication.
# Install sequenzo from the release/tag documented in the replication README
# if it is not yet on PyPI.

numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.14.1
pytest==8.3.3

# sequenzo==<pin after release>  # Required; install per replication README
